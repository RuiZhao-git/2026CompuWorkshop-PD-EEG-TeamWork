# Team meeting agenda, 2026-05-13

## Repo walkthrough

Private repo: `2026CompuWorkshop-PD-EEG-modeling`. Top-level `README.md` + a README in each subfolder.

- `src/`: reusable functions. `features.py` done. `model.py`, `fitting.py` to write.
- `scripts/`: numbered pipeline. `01_extract_features.py` done. `02_simulate_network.py`, `03_fit_per_subject.py` to write.
- `notebooks/`: exploration, prefix files with your initials.
- `slides/`: Marp source for class presentations.
- `docs/`: meeting notes and decisions.
- `results/`: outputs. `features.csv` is in.

## Setup for new collaborators

1. `pip install -r requirements.txt` (a fresh conda env is recommended)
2. Copy `.env.example` to `.env`, set `PD_DATA_DIR` to your local data path
3. Data files stay local, not in repo
4. `results/features.csv` is already committed, no need to re-run `01_extract_features.py`

## Group summary from features.csv (n=149)

| Feature | Control | PD | Matches hypothesis direction |
|---------|---------|-----|-----------------------------|
| alpha peak Hz (median) | 9.25 | 8.00 | yes |
| theta relative power | 0.13 | 0.23 | yes |
| beta relative power | 0.24 | 0.18 | yes |
| alpha relative power | 0.25 | 0.24 | no (nearly equal) |

The alpha rhythm slows but its relative power does not drop. Suggests rewording "alpha decrease" in the hypothesis to "alpha peak slowing".

## Remaining work before 22.05

Code:
- `src/model.py`: Wilson-Cowan network via neurolib
- `scripts/02_simulate_network.py`: scan tau_I, record simulated alpha peak
- `src/fitting.py` and `scripts/03_fit_per_subject.py`: per-subject tau_I from curve inversion
- A statistical comparison script or notebook for the group test

Deliverables for 22.05:
- 3 slides: hypothesis, model, next steps
- Optional 4th slide with open questions for the podium

## Decisions to make today

1. Hypothesis wording: "alpha decrease" to "alpha peak slowing"?
2. Number of network nodes N
3. Coupling: all-to-all vs nearest-neighbor vs structural
4. Role assignments

## Roles

- Model implementation:
- Fitting:
- Statistics and slides:
- Coordination:

## Next meeting

Date:
